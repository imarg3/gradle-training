tasks.register("defaultGreeting", Greeting)

tasks.register("customGreeting", Greeting) {
    message = "Hello from me"
}

abstract class Greeting extends DefaultTask {
    @Input
    String message = "Hello from Gradle"

    @TaskAction
    void printGreeting() {
        println message
    }
}
